# The rule set application_base ships to every package and application of the
# stack. Preview fixes with `dart fix --dry-run`, apply them with
# `dart fix --apply`.
include: package:application_base/analysis_options.yaml
